import Component from "$store/components/NudgifyCart.tsx";
import type { Props } from "$store/components/NudgifyCart.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
