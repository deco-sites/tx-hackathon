import Component from "$store/components/NudgifyPDP.tsx";
import type { Props } from "$store/components/NudgifyPDP.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
