import Component from "$store/components/Nudgify.tsx";
import type { Props } from "$store/components/Nudgify.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
